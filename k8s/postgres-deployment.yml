apiVersion: apps/v1
kind: Deployment
metadata:
  name:  devboard-postgres-deployment
  namespace: devboard
  labels:
    app:  devboard-postgres
spec:
  selector:
    matchLabels:
      app: devboard-postgres
  replicas: 1
  # strategy:
  #   rollingUpdate:
  #     maxSurge: 25%
  #     maxUnavailable: 25%
  #   type: RollingUpdate
  template:
    metadata:
      labels:
        app:  devboard-postgres
    spec:
      containers:
      - name: postgres  #in postgres URl we have give postgres not devboard postgres.
        image: postgres:16-alpine
        ports:
          - containerPort: 5432
        env:
          - name: POSTGRES_DB            # Name inside the container
            valueFrom:
              secretKeyRef:
                name: devboard-secrets                # Name of your Secret file meta
                key: POSTGRES_DB                      # Key inside the Secret

          - name: POSTGRES_PASSWORD      # Name inside the container
            valueFrom:
              secretKeyRef:
                name: devboard-secrets                # Name of your Secret file meta
                key: POSTGRES_PASSWORD                # Key inside the Secret

          - name: POSTGRES_USER          # Name inside the container 
            valueFrom:  
              configMapKeyRef:
                name: devboard-configmap              # Name of your ConfigMap file meta
                key: POSTGRES_USER                    # Key inside the ConfigMap
          
    volumes:
      - name: init  #any volume name
        configMap: postgres-init   #from configmap-ini.yml meta


          
    # spec:
      # initContainers:
        # Init containers are exactly like regular containers, except:
          # - Init containers always run to completion.
          # - Each init container must complete successfully before the next one starts.
      # containers:
      # - name:  devboard-postgres
      #   image:  vishaldocker85/devboard-postgres:latest
      #   resources:
      #     requests:
      #       cpu: 100m
      #       memory: 100Mi
      #     limits:
      #       cpu: 100m
      #       memory: 100Mi
      #   livenessProbe:
      #     tcpSocket:
      #       port: 80
      #     initialDelaySeconds: 5
      #     timeoutSeconds: 5
      #     successThreshold: 1
      #     failureThreshold: 3
      #     periodSeconds: 10
      #   readinessProbe:
      #     httpGet:
      #       path: /_status/healthz
      #       port: 80
      #     initialDelaySeconds: 5
      #     timeoutSeconds: 2
      #     successThreshold: 1
      #     failureThreshold: 3
      #     periodSeconds: 10
      #   env:
      #   - name: DB_HOST
      #     valueFrom:
      #       configMapKeyRef:
      #         name: MYAPP
      #         key: DB_HOST
      #   ports:
      #   - containerPort:  80
      #     name:  MYAPP
      #   volumeMounts:
      #   - name: localtime
      #     mountPath: /etc/localtime
      # volumes:
      #   - name: localtime
      #     hostPath:
      #       path: /usr/share/zoneinfo/Asia/Shanghai
      # restartPolicy: Always